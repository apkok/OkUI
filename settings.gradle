pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "OkUI"
include ':app'

include ':oklibrary'
project(':oklibrary').projectDir = "../OkLibrary/oklibrary" as File
include ':okui'
